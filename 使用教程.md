# 个人网站零基础使用教程

本教程假设你**没有编程基础**。按顺序做即可完成：在电脑里改内容 → 自己先看效果 → 再发布到互联网。

---

## 一、先搞懂这几件事（不用背）

| 说法 | 你可以把它理解成 |
|------|------------------|
| **网站** | 很多张「网页」放在一起，用浏览器打开就能看到 |
| **GitHub** | 一个放网站文件的网络硬盘，还能免费帮你把网站挂到网上 |
| **这个项目的文件夹** | 你电脑里的 `lynx.github.io` 文件夹，里面就是网站的全部文件 |
| **`.html` 文件** | 某一页网页的「稿子」，用记事本也能打开改字 |
| **发布 / 部署** | 把改好的文件上传到 GitHub，别人才能通过网址访问 |

你的网站上线后，地址一般是：**https://Lynx4506.github.io**（与 GitHub 用户名一致）。

---

## 二、你需要准备什么

1. **一台 Windows 电脑**（本教程按 Windows 写）。  
2. **自己的 GitHub 账号**（你当前用户名为 `Lynx4506`）。  
3. **网站文件所在位置**：例如 `C:\Users\LYNX\lynx.github.io`（若你移动过文件夹，以后面的实际路径为准）。

---

## 三、最简单的看法：先确认「能打开网页」

1. 打开文件夹 **`lynx.github.io`**。  
2. 双击 **`index.html`**。  
3. 一般会默认用 **Edge** 或 **Chrome** 打开，能看到首页即表示文件没问题。

**局限：** 这样看有时和「真正网站」略有差别；想更稳妥请用下面「五、在自己电脑预览整站」。

---

## 四、怎么改文字和图片（最重要规则）

### 4.1 用什么软件打开文件？

任选其一即可：

- **记事本**：开始菜单搜索「记事本」，打开后：文件 → 打开 → 选 `index.html`（右下角文件类型选「所有文件」）。  
- **Cursor / VS Code**：若你已在用，用「打开文件」选择 `.html` 即可。

### 4.2 改完一定要「保存」

按 **Ctrl + S**（或菜单里的保存）。不保存，浏览器里看不到新内容。

### 4.3 黄金规则（避免把网页弄坏）

网页里会有很多用尖括号包起来的记号，例如：

```html
<p>这里是可以改的文字</p>
```

- **只改** `>` 和 `</` 之间的**中文或英文**。  
- **不要删** `<p>`、`</p>` 这类符号，也不要改它们的顺序。  
- 不确定时，**少改、先备份**：复制一份整个文件夹到桌面当备份。

更细的「改哪几个文件」见同文件夹里的 **`如何修改网站.md`**（偏「改哪里」清单）。本教程偏「怎么操作电脑」。

---

## 五、在自己电脑预览整站（推荐）

预览可以像在真实网站上一样点菜单、进子页面，减少「上传后才发现不对」的情况。

### 5.1 安装 Python（二选一）

脚本 **`serve.ps1`** 会用 Python 在本地开一个临时小网站。

1. 打开：https://www.python.org/downloads/  
2. 下载并安装。安装时**勾选** “Add Python to PATH”（添加到环境变量），再点安装。  
3. 关掉已打开的终端窗口，重新开一次（下面第 5.3 步）。

若你已安装 **Node.js**，也可在文件夹里打开终端，执行：  
`npx -y serve . -l 8080`  
效果类似，浏览器同样访问 `http://127.0.0.1:8080`。

### 5.2 打开 Windows 终端（PowerShell）

1. 按键盘 **Win 键**，输入 **PowerShell**。  
2. 点击 **Windows PowerShell**（蓝色窗口）。

### 5.3 进入网站文件夹

在蓝色窗口里**一行一行**输入下面命令，每行输完按 **回车**（请把路径改成你电脑上的真实路径）：

```powershell
cd C:\Users\LYNX\lynx.github.io
```

若提示找不到路径，说明文件夹不在 C 盘或用户名不同：请用资源管理器打开 `lynx.github.io` 文件夹，在地址栏**单击一下**复制完整路径，在 PowerShell 里输入：

```powershell
cd 粘贴你复制的路径
```

### 5.4 允许运行脚本（仅本次窗口有效）

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

### 5.5 启动本地预览

```powershell
.\serve.ps1
```

窗口里若出现类似 **Serving at http://127.0.0.1:8080**，打开浏览器（Edge / Chrome），地址栏输入：

**http://127.0.0.1:8080**

回车即可浏览全站。

### 5.6 关掉预览

回到蓝色窗口，按 **Ctrl + C**，预览服务就停止了。

---

## 六、发布到 GitHub（让别人能访问）

下面分「第一次要做的事」和「以后每次更新」。

### 6.1 第一次需要安装的工具

1. **Git（版本管理）**  
   - 打开：https://git-scm.com/download/win  
   - 下载安装，一路「下一步」即可（保持默认选项对新手最省事）。

2. **GitHub CLI（命令行里操作 GitHub）**  
   - 打开：https://cli.github.com/  
   - 安装完成后，打开 **PowerShell**，执行：

```powershell
gh auth login
```

按屏幕提示：用浏览器登录 GitHub、授权。做到显示登录成功为止。

> 若 `git` 或 `gh` 提示「不是内部或外部命令」，说明安装后未重启终端或安装未完成，请关掉所有 PowerShell 窗口再开一次重试。

### 6.2 第一次：在 GitHub 上打开「网页托管」（Pages）

只需做一次（每个仓库一次）：

1. 浏览器打开 GitHub，进入仓库 **`Lynx4506.github.io`**（若没有，运行一次下面的 `deploy.ps1` 往往会自动创建）。  
2. 点仓库顶部的 **Settings（设置）**。  
3. 左侧找到 **Pages**。  
4. **Build and deployment** 里：  
   - Source 选 **Deploy from a branch**  
   - Branch 选 **main**，文件夹选 **/ (root)**  
5. 保存后等待几分钟，再访问 **https://Lynx4506.github.io**。

### 6.3 上传网站文件（部署脚本）

1. **PowerShell** 进入网站文件夹（同第五节的 `cd`）。  
2. 执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\deploy.ps1
```

3. 若提示登录、权限或网络错误，把**完整英文报错**复制下来搜索，或请身边懂 Git 的朋友帮你看一次。

**说明：** 脚本会根据当前登录的 GitHub 账号，把网站推到名为 **`你的用户名.github.io`** 的仓库。你的用户名为 `Lynx4506` 时，即 **`Lynx4506.github.io`**。

### 6.4 以后每次「改完想更新网上版本」

1. 保存好你改过的 `.html` 等文件。  
2. 再执行一次 **`deploy.ps1`**（同上）。  
3. 等 1～3 分钟，用浏览器打开 **https://Lynx4506.github.io**，必要时 **Ctrl + F5** 强制刷新。

---

## 七、常见问题（零基础版）

**问：我双击 `serve.ps1` 或 `deploy.ps1` 一闪就没了？**  
答：请在 **PowerShell** 里先 `cd` 到网站文件夹，再输入 `.\serve.ps1` 或 `.\deploy.ps1`，这样才能看到提示和错误信息。

**问：改完浏览器还是旧内容？**  
答：先确认已 **Ctrl + S** 保存文件；再试 **Ctrl + F5** 强制刷新；本地预览请确认 `serve.ps1` 仍在运行且地址是 `127.0.0.1:8080`。

**问：图片不显示？**  
答：检查照片是否真的在 `assets\photos` 里；文件名是否和网页里写的一致（包括后缀 `.jpg` / `.png`）；路径是否写错。

**问：页面排版全乱了？**  
答：多半是删改了尖括号里的内容。用备份文件夹恢复，或从 GitHub 网页上重新下载一份未改坏的文件。

**问：我完全不想用命令行？**  
答：可以只用「三、双击 index.html」在本地看；但**发布到网上**通常仍需要一次命令行或请朋友代为执行 `deploy.ps1`。也可以改用 GitHub 网页的「上传文件」功能手动上传（步骤较多，适合以后再学）。

---

## 八、文档对照

| 文档 | 适合用来 |
|------|----------|
| **使用教程.md**（本文） | 从零学：开终端、预览、发布、排错思路 |
| **如何修改网站.md** | 快速查：改哪个文件、改首页/照片/文章 |

按顺序：**先看本文 → 改内容时打开「如何修改网站」对照 → 预览 → 再发布**。

祝你使用顺利。
