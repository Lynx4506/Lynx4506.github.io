# 如何修改这个个人网站

这份说明写给**不擅长写代码**的朋友：你只需要用记事本或 Cursor 打开网页文件，改文字、换图片链接即可。

**第一次使用？** 请先阅读同文件夹里的 **[使用教程.md](./使用教程.md)**（从零讲解：打开终端、本地预览、发布到 GitHub）。

---

## 网站文件都在哪里？

所有内容在文件夹 **`lynx.github.io`** 里，主要改下面这些文件：

| 文件 | 作用 |
|------|------|
| `index.html` | 首页：自我介绍、一句话介绍 |
| `photos.html` | 照片页：展示相册或单张照片 |
| `writing.html` | 文章列表：显示你写了哪些文章 |
| `articles` 文件夹里的 `.html` | 每一篇具体文章的正文 |

样式（颜色、字体、排版）在 **`css/style.css`**，一般不用动。

---

## 怎么改「个人信息」？

### 1. 改首页介绍（最重要）

1. 用记事本或 Cursor 打开 **`index.html`**。  
2. 找到页面中间大标题下面的几段**中文或英文文字**，直接改成你自己的介绍。  
3. 保存文件后，用浏览器重新打开页面（或刷新）即可看到效果。

**小提示：** 不要删掉两边的尖括号标签（例如 `<p>...</p>`），只改标签**中间**的文字。

### 2. 换首页头像图

1. 在 `index.html` 里找到很长的一串以 `https://` 开头的图片地址。  
2. 你可以：  
   - 把自己的照片放到 **`assets/photos`** 文件夹里，例如命名为 `me.jpg`；  
   - 然后把那一串地址改成：`assets/photos/me.jpg`（文件名要和实际一致）。

### 3. 改照片页

1. 打开 **`photos.html`**。  
2. 每一张照片对应一块「卡片」：里面有标题、说明和图片地址。  
3. 把图片地址换成你自己的（可以放在 `assets/photos` 里，写法同上）。  
4. 标题和说明文字直接改成你的相册名、拍摄地点等。

页面顶部的分类按钮（日常生活 / 旅行 / 猫咪）对应每张照片上的一个标记，若你改了分类名字，需要同时改按钮上的文字和照片块里的分类，保持一致即可（不懂可以只改文字，少动结构）。

### 4. 写文章、改文章列表

**新增一篇文章：**

1. 复制 **`articles`** 里任意一个 `.html` 文件，改个新文件名（建议用英文，例如 `my-trip.html`）。  
2. 打开新文件，改标题、日期和正文段落（同样只改标签中间的文字）。  
3. 打开 **`writing.html`**，照着现有条目复制一整块「文章卡片」，把链接改成你的新文件名，标题和摘要改成你的文章信息。

**只改已有示例文章：**  
直接打开 `articles` 里对应的 `.html`，改里面的文字即可。

---

## 改完怎么在自己电脑上看？

在项目文件夹里运行 **`serve.ps1`**（若不会运行，可请身边懂电脑的朋友帮忙执行一次），浏览器打开提示的地址（一般是 `http://127.0.0.1:8080`）即可预览。

---

## 改完怎么发布到 GitHub？

确认本地预览满意后，在项目文件夹里运行 **`deploy.ps1`**（需已安装 GitHub 官方命令行工具并登录账号）。  
首次发布还须在 GitHub 网页上：**仓库 → Settings → Pages**，选择用 **`main`** 分支、**根目录** 部署。  
发布后你的网站地址一般为：**https://Lynx4506.github.io**

---

## 遇到问题怎么办？

- 改完网页不更新：按 **Ctrl + F5** 强制刷新浏览器。  
- 图片不显示：检查文件名、路径是否写对，照片是否真的放在 `assets/photos` 里。  
- 页面变乱：多半是不小心删掉了 `<` `>` 一类的符号，可用 Git 恢复文件，或从备份里复制一份重来。

如需大改版式或加新功能，再考虑请开发者协助即可；日常只换文字和图片，按上面步骤即可。
